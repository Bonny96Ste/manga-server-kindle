local _ = require("gettext")
return {
    fullname = _("MangaBridge"),
    description = _([[Browse MangaBridge, download chapters for offline reading, track reading progress, continue across devices, and automatically advance between chapters.]]),
}
