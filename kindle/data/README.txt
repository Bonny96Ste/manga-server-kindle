MangaBridge data folder

Edit config.lua and set:
- server_url: your MangaDL Ultimate server URL
- api_token: the KINDLE_API_TOKEN configured on the server
- username/password: only if Basic authentication is enabled

Downloaded chapters are stored under library/.
Temporary metadata is stored under cache/.
